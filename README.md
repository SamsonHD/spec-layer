## Spec Layer

Spec Layer is the approved contract between design and code: your design system as text — reviewed by humans, executed by AI, never out of sync. It provides a single source of truth that bridges design intent and implementation, keeping components, tokens, and patterns consistent across every surface without manual reconciliation.

For the full specification, see [spec/SPEC.md](spec/SPEC.md).
