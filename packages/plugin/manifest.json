{
  "name": "Spec Layer",
  "id": "spec-layer-dev",
  "api": "1.0.0",
  "main": "dist/main.js",
  "ui": "dist/ui.html",
  "editorType": ["figma"],
  "networkAccess": {
    "allowedDomains": ["http://localhost:3000"],
    "reasoning": "The plugin POSTs extracted component specs to a locally running docs web app (Send to docs), which defaults to http://localhost:3000. Widen this list explicitly if/when a production docs host exists.",
    "devAllowedDomains": ["http://localhost:3000"]
  }
}
